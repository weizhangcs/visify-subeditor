# =============================================================================
# === 1. Dependencies & Lockfiles
# =============================================================================
# The node_modules directory, which contains all installed packages.
# This should always be ignored as it can be regenerated by running `yarn install`.
/node_modules

# =============================================================================
# === 2. Nuxt.js Generated Files & Build Output
# =============================================================================
# The .nuxt directory is generated during development and contains the compiled app.
/.nuxt

# The .output directory is generated when building the app for production.
/.output

# A common directory for build outputs.
/dist

# =============================================================================
# === 3. Logs
# =============================================================================
# Log files generated by package managers or other tools.
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# =============================================================================
# === 4. Environment Variables
# =============================================================================
# Ignore all local environment files. These files contain sensitive information
# (API keys, database passwords) and are machine-specific.
# A `.env.example` file should be committed instead as a template.
.env
.env.*
!.env.example
!.env.production.example
!.env.development.example

# =============================================================================
# === 5. IDE & Editor Configurations
# =============================================================================
# WebStorm / JetBrains IDE project files. The manifest confirms its existence.
.idea/

# VSCode editor files (good practice for team collaboration).
.vscode/

# =============================================================================
# === 6. OS Generated Files
# =============================================================================
# macOS specific files. The manifest shows these exist in the original project[cite: 10, 27, 34].
.DS_Store
.AppleDouble
.LSOverride

# Windows specific files.
Thumbs.db
ehthumbs.db
desktop.ini

# =============================================================================
# === 7. Temporary & Local Files
# =============================================================================
# Our temporary analysis file. This should not be part of the source code.
file_manifest.txt

# Other temporary file formats.
*.tmp
*.swp